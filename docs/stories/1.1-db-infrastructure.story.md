# Story 1.1: CRM Database Infrastructure

## Status
Done ✅

## Executor Assignment
executor: "@data-engineer"
quality_gate: "@dev"
quality_gate_tools: ["rspec", "db-audit"]

## Story
**As a** Developer,
**I want** to implement the base tables and performance indices for the CRM,
**so that** the system can store and filter conversations as deals efficiently.

## Acceptance Criteria
1. Tabela `crm_pipelines` criada com `name`, `position` (default 0), `active` (default true) e `account_id`.
2. Tabela `crm_pipeline_stages` criada com `name`, `color`, `position` (default 0), `active` (default true), `account_id` e `pipeline_id`.
3. Coluna `pipeline_stage_id` e `pipeline_id` adicionadas em `conversations` (FKs).
4. Campo `conversations_count` (counter cache) adicionado em `crm_pipeline_stages`.
5. Índice composto em `conversations`: `(account_id, pipeline_stage_id, last_activity_at DESC)`.
6. Todas as migrations devem ser reversíveis.

## 🤖 CodeRabbit Integration
> **CodeRabbit Integration**: Disabled
>
> CodeRabbit CLI is not enabled in `core-config.yaml`.
> Quality validation will use manual review process only.
> To enable, set `coderabbit_integration.enabled: true` in core-config.yaml

## Tasks / Subtasks
- [x] Task 1: Migration `create_crm_pipelines` (AC: #1)
  - [x] Implement name, position, active, account_id fields.
- [x] Task 2: Migration `create_crm_pipeline_stages` (AC: #2)
  - [x] Implement link to crm_pipelines.
- [x] Task 3: Migration `add_crm_fields_to_conversations` (AC: #3, #4)
  - [x] Add FKs and counter_cache field.
- [x] Task 4: Performance Indexing (AC: #5)
  - [x] Create the composite index for the Kanban board.
- [x] Task 5: Migration Testing (AC: #6)
  - [x] Logic verified for Rails 7.0 and PostgreSQL.

## Dev Notes
- **Source Reference**: `docs/database/DB-AUDIT.md`
- **Architecture Context**: The system uses `account_id` as the primary partition key for almost all queries. Ensure all new tables have `account_id` and proper indexes.
- **Counter Cache**: Rails' native counter cache expects the column name to be the pluralized model name + `_count`.

### Testing
- **Standards**: Follow the `docs/framework/testing-strategy.md`.
- **Requirements**: Use `bundle exec rspec` for testing migration integrity if applicable.

## Change Log
| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-04-27 | 1.0 | Initial story creation using v2.0 template | River (@sm) |
| 2026-04-27 | 1.1 | Migration files created and fixed with performance indices | Dara (@data-engineer) |

## Dev Agent Record
- **Agent Model Used**: Gemini 2.0 Flash (Antigravity)
- **File List**:
  - `db/migrate/20260427130000_create_crm_pipelines.rb`
  - `db/migrate/20260427130100_create_crm_pipeline_stages.rb`
  - `db/migrate/20260427130200_add_pipeline_stage_id_to_conversations.rb`
- **Completion Notes**:
  - Migrations created with standard Rails 7.0 syntax.
  - Performance index `index_conversations_on_crm_pipeline_lookup` added with `order: { last_activity_at: :desc }` for optimized board sorting.
  - Counter cache `conversations_count` implemented on `crm_pipeline_stages`.

## QA Results
- **Gate Decision**: PASS ✅
- **Review Date**: 2026-04-27
- **QA Agent**: Quinn (@qa)
- **Technical Review**:
  - **Database Integrity**: All foreign keys are properly constrained to their respective tables.
  - **Performance**: Composite index `index_conversations_on_crm_pipeline_lookup` verified. It correctly supports the Kanban view filtering by `account_id` + `pipeline_stage_id` and sorting by `last_activity_at DESC`.
  - **Scalability**: Use of `conversations_count` counter cache on stages prevents N+1 count queries during board render.
  - **Risk Assessment**: LOW. Standard Rails migrations with proper indexing.
- **Recommended Tests**:
  - Unit test for `CrmPipelineStage` to verify counter_cache increment/decrement when a conversation is moved.
